🧪 Add edge case tests for log_checkpoint metadata handling

🎯 What: The log_checkpoint function lacked edge case tests for the metadata argument, specifically when it is None, an empty dictionary, or contains overlapping keys with the default entry fields.
📊 Coverage: Added three tests: test_log_checkpoint_metadata_none, test_log_checkpoint_metadata_empty, and test_log_checkpoint_metadata_overlapping_keys.
✨ Result: Improved test coverage and confidence in the metadata handling behavior of log_checkpoint.
